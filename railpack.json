{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "steps": {
    "build": {
      "commands": [
        "cd frontend && npm install -g pnpm --silent && pnpm install --frozen-lockfile && pnpm build && cd .."
      ]
    }
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}
